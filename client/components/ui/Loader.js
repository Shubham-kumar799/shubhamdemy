//components
import { Spin } from 'antd';

const Loader = () => {
  return (
    <div className="items-center flex flex-1 justify-center h-screen">
      <Spin />
    </div>
  );
};

export default Loader;
